---
phase: 01-core-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [Java/src/main/java/com/dev/view/ResumeEditorView.java, Java/src/main/java/com/dev/util/AnimationUtil.java, Java/src/main/java/com/dev/view/components/ModernPanel.java, Java/src/main/java/com/dev/view/components/AnimatedMenu.java]
autonomous: true
requirements: [1.1, 1.3, 1.4, 2.1]

must_haves:
  truths:
    - "ResumeEditorView uses UITheme helpers for tables and scrolling"
    - "Dead code identified in research is completely removed"
    - "No commented-out code or unreferenced methods remain in the UI layer"
  artifacts:
    - path: "Java/src/main/java/com/dev/view/ResumeEditorView.java"
      provides: "Themed and cleaned main editor view"
---

<objective>
Finalize the UI foundation by refactoring the main application view and purging all identified dead code and redundant methods. This ensures the codebase is lean, self-documenting, and consistent with the new theme architecture.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/phase-1.md
@.planning/REQUIREMENTS.md
@Java/src/main/java/com/dev/view/ResumeEditorView.java
@Java/src/main/java/com/dev/util/AnimationUtil.java
@.planning/phases/01-core-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ResumeEditorView</name>
  <files>Java/src/main/java/com/dev/view/ResumeEditorView.java</files>
  <action>
    1. Replace all hardcoded 'new Color(...)' instances with UITheme constants (BACKGROUND, SURFACE, FOREGROUND, etc.).
    2. Replace direct JTable styling (setSelectionBackground, setRowHeight, etc.) with UITheme.styleTable(table) for:
       - summaryTable
       - educationTable
       - experienceTable
       - skillsTable
    3. Identify patterns of 'new RoundedScrollPane(panel)' combined with empty borders/paddings and replace them with UITheme.wrapInScrollPanel(panel).
    4. CONSTRAINT: Do not include any comments in the code.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>ResumeEditorView is refactored to use UITheme helpers and constants.</done>
</task>

<task type="auto">
  <name>Task 2: Purge Dead UI Code and Components</name>
  <files>Java/src/main/java/com/dev/util/AnimationUtil.java, Java/src/main/java/com/dev/view/components/ModernPanel.java, Java/src/main/java/com/dev/view/components/AnimatedMenu.java</files>
  <action>
    1. Delete the following unreferenced methods from AnimationUtil.java:
       - fadeIn(JComponent, int)
       - slideIn(JComponent, String, int)
       - animateColorTransition(JComponent, Color, Color, int)
    2. Remove the 'radiance-animation' related imports from AnimationUtil.java if they become unused.
    3. Delete the file Java/src/main/java/com/dev/view/components/AnimatedMenu.java.
    4. Remove the redundant setArc(int) method from ModernPanel.java (it is an alias for setCornerRadius).
    5. CONSTRAINT: Do not include any comments in the code.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>Identified dead code and unused components are removed.</done>
</task>

<task type="auto">
  <name>Task 3: ResumeEditorView Cleanup</name>
  <files>Java/src/main/java/com/dev/view/ResumeEditorView.java</files>
  <action>
    1. Remove the unused private method 'addField' (note: 'addModernField' is the one being used).
    2. Search for and remove any commented-out calls to 'AnimationUtil.pulse(...)'.
    3. Ensure no other commented-out code remains in this file.
    4. CONSTRAINT: Do not include any comments in the code.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>ResumeEditorView is clean of unused methods and commented-out logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System -> Filesystem | Deletion of files (AnimatedMenu) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Build System | mitigate | Ensure mvn compile passes after deletions to avoid breaking the build |
</threat_model>

<verification>
Verify the application compiles and runs without any missing component errors.
</verification>

<success_criteria>
Main view is refactored, dead code is purged, and the project builds successfully.
</success_criteria>

<output>
Create .planning/phases/01-core-foundation/01-03-SUMMARY.md when done
</output>
