---
phase: 01-core-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [Java/src/main/java/com/dev/view/ResumeEditorView.java, Java/src/main/java/com/dev/util/AnimationUtil.java, Java/src/main/java/com/dev/view/components/ModernPanel.java, Java/src/main/java/com/dev/view/components/AnimatedMenu.java, Java/src/main/java/com/dev/view/components/BannerPanel.java, Java/pom.xml]
autonomous: true
requirements: [1.1, 1.3, 1.4, 2.1, 2.3]

must_haves:
  truths:
    - "ResumeEditorView uses UITheme helpers for tables and scrolling"
    - "Dead code identified in research is completely removed"
    - "radiance-animation dependency is removed from pom.xml"
    - "BannerPanel contains zero network or image loading logic"
    - "No comments remain in refactored UI files"
  artifacts:
    - path: "Java/src/main/java/com/dev/view/ResumeEditorView.java"
      provides: "Themed and cleaned main editor view"
    - path: "Java/src/main/java/com/dev/view/components/BannerPanel.java"
      provides: "Simplified, local-only banner"
  key_links:
    - from: "Java/src/main/java/com/dev/view/ResumeEditorView.java"
      to: "Java/src/main/java/com/dev/util/UITheme.java"
      via: "UITheme helpers (styleTable, wrapInScrollPanel)"
      pattern: "UITheme\\.styleTable"
    - from: "Java/pom.xml"
      to: "Java/src/main/java/com/dev/util/AnimationUtil.java"
      via: "Dependency removal impact"
      pattern: "<artifactId>radiance-animation</artifactId>"
---

<objective>
Finalize the UI foundation by refactoring the main application view, purging all identified dead code, simplifying the BannerPanel to remove network/image dependencies, and removing the radiance-animation dependency from the build system. Enforce the 'no-comments' constraint globally for all touched files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/phase-1.md
@.planning/REQUIREMENTS.md
@Java/src/main/java/com/dev/view/ResumeEditorView.java
@Java/src/main/java/com/dev/util/AnimationUtil.java
@Java/src/main/java/com/dev/view/components/BannerPanel.java
@Java/pom.xml
@.planning/phases/01-core-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ResumeEditorView and Cleanup Comments</name>
  <files>Java/src/main/java/com/dev/view/ResumeEditorView.java</files>
  <action>
    1. Replace all hardcoded 'new Color(...)' instances with UITheme constants (BACKGROUND, SURFACE, FOREGROUND, etc.).
    2. Replace direct JTable styling (setSelectionBackground, setRowHeight, etc.) with UITheme.styleTable(table) for all data tables.
    3. Replace 'new RoundedScrollPane(panel)' + border patterns with UITheme.wrapInScrollPanel(panel).
    4. LEGACY CLEANUP: Remove ALL existing comments from ResumeEditorView.java.
  </action>
  <verify>
    <automated>mvn compile && grep -c "//" Java/src/main/java/com/dev/view/ResumeEditorView.java | grep "^0$"</automated>
  </verify>
  <done>ResumeEditorView is refactored, uses UITheme helpers, and is comment-free.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify BannerPanel and Purge Dead UI Code</name>
  <files>Java/src/main/java/com/dev/view/components/BannerPanel.java, Java/src/main/java/com/dev/util/AnimationUtil.java, Java/src/main/java/com/dev/view/components/ModernPanel.java, Java/src/main/java/com/dev/view/components/AnimatedMenu.java</files>
  <action>
    1. Refactor BannerPanel.java:
       - Remove ALL network-related code (e.g., URL fetching).
       - Remove ALL image loading logic.
       - Simplify it to a basic styled panel using UITheme colors (per Req 1.3).
    2. Delete the following unreferenced methods from AnimationUtil.java:
       - fadeIn, slideIn, animateColorTransition.
    3. Delete the file Java/src/main/java/com/dev/view/components/AnimatedMenu.java.
    4. Remove redundant setArc(int) from ModernPanel.java.
    5. LEGACY CLEANUP: Remove ALL existing comments from these files.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>BannerPanel is simplified (no network/images), dead code is purged, and touched files are comment-free.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup Build System and Final Code Audit</name>
  <files>Java/pom.xml, Java/src/main/java/com/dev/view/ResumeEditorView.java</files>
  <action>
    1. Remove the 'radiance-animation' dependency from pom.xml (per Req 1.3).
    2. Remove the unused private method 'addField' from ResumeEditorView.java.
    3. Final grep for any remaining commented-out code in ResumeEditorView.java or AnimationUtil.java and remove it.
  </action>
  <verify>
    <automated>mvn clean compile && grep -c "radiance-animation" Java/pom.xml | grep "^0$"</automated>
  </verify>
  <done>Build system is cleaned, and final code audit confirms no dead code or comments remain in core UI files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System -> Filesystem | Deletion of files and modification of pom.xml |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Build System | mitigate | Ensure mvn compile passes after deletions to avoid breaking the build |
</threat_model>

<verification>
Verify the application compiles and runs without any missing component errors. Confirm BannerPanel is functional without images.
</verification>

<success_criteria>
Main view is refactored, dead code and radiance-animation are purged, BannerPanel is simplified, and the project builds successfully. All touched files are comment-free.
</success_criteria>

<output>
Create .planning/phases/01-core-foundation/01-03-SUMMARY.md when done
</output>
