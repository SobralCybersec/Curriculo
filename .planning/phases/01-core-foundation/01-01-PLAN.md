---
phase: 01-core-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Java/src/main/java/com/dev/util/UITheme.java, Java/src/main/java/com/dev/Main.java, Java/src/test/java/com/dev/util/UIThemeTest.java]
autonomous: true
requirements: [1.1, 2.1, 2.2]

must_haves:
  truths:
    - "UITheme centralizes all colors from RESEARCH.md"
    - "Main.java is minimal, delegating Look&Feel to UITheme"
    - "UITheme helpers are verified via property-based tests"
  artifacts:
    - path: "Java/src/main/java/com/dev/util/UITheme.java"
      provides: "Centralized styling logic"
    - path: "Java/src/test/java/com/dev/util/UIThemeTest.java"
      provides: "Property-based verification of UI helpers"
---

<objective>
Establish the core UI foundation by implementing the UITheme utility and refactoring the application entry point. This plan centralizes styling constants and provides verified helpers for UI consistency.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/phase-1.md
@Java/src/main/java/com/dev/Main.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UITheme and Refactor Main</name>
  <files>Java/src/main/java/com/dev/util/UITheme.java, Java/src/main/java/com/dev/Main.java</files>
  <action>
    1. Create UITheme.java in com.dev.util with color constants (BACKGROUND, SURFACE, BORDER, SELECTED, FOREGROUND, ACCENT) using hex values from RESEARCH.md.
    2. Implement applyFlatLaf() in UITheme:
       - Call FlatDarkLaf.setup().
       - Move all UIManager.put calls from Main.java to this method, using the new color constants.
       - Use UITheme constants for repeated Color objects.
    3. Refactor Main.java:
       - Replace the try-catch block containing FlatLaf setup and UIManager calls with UITheme.applyFlatLaf().
    4. CONSTRAINT: Do not include any comments in the code.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>UITheme.java exists with correct constants and Main.java is refactored to use it.</done>
</task>

<task type="auto">
  <name>Task 2: Implement UITheme Styling Helpers</name>
  <files>Java/src/main/java/com/dev/util/UITheme.java</files>
  <action>
    1. Implement styleTable(JTable table) in UITheme:
       - Set selection background to UITheme.SELECTED.
       - Set grid/border colors as appropriate for the dark theme.
       - Set row height to 28 (per Main.java legacy).
    2. Implement wrapInScrollPanel(JPanel panel) in UITheme:
       - Return a RoundedScrollPane wrapping the panel.
       - Apply an empty border of 20px on all sides.
    3. CONSTRAINT: Do not include any comments in the code.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>UITheme.java contains styleTable and wrapInScrollPanel methods.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Add Property-Based Tests for UITheme</name>
  <files>Java/src/test/java/com/dev/util/UITheme.java, Java/src/test/java/com/dev/util/UIThemeTest.java</files>
  <behavior>
    - styleTable(JTable) must always set selectionBackground to UITheme.SELECTED.
    - wrapInScrollPanel(JPanel) must return a JScrollPane containing the panel in its viewport.
  </behavior>
  <action>
    1. Create UIThemeTest.java using jqwik (@Property).
    2. Test that styleTable correctly applies theme colors regardless of table state.
    3. Test that wrapInScrollPanel correctly sets up the component hierarchy.
    4. CONSTRAINT: Do not include any comments in the code.
  </action>
  <verify>
    <automated>mvn test -Dtest=UIThemeTest</automated>
  </verify>
  <done>Tests pass and cover the helper methods in UITheme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Theme | Theme constants must be immutable to prevent runtime style corruption |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | UITheme Constants | mitigate | Use 'public static final' for all constants |
</threat_model>

<verification>
Check that the application still launches with the correct theme after refactoring Main.
</verification>

<success_criteria>
Main.java is simplified, UITheme provides all styling constants/helpers, and helpers are tested.
</success_criteria>

<output>
Create .planning/phases/01-core-foundation/01-01-SUMMARY.md when done
</output>
